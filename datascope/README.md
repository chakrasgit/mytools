# DataScope

A Streamlit app for quick exploratory data analysis.

Upload a CSV or Excel file (with multiple sheets if needed), pick which sheet(s) to explore, and get a structured report for each: column types (categorical, numerical, time-based), null counts, unique values, and key stats. Download any report as CSV.

